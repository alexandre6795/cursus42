dans la verif pid == 0
dup2 pipe entree sur stdin
dup2 pipe sortie sur stdout
puis l'envoyer vers une fonction d'exec

fonction qui cherche le path correct
ensuite envoie vers execve
si le process va plus loin que execve faire une erreur et penser a exit

ls:
char * = /bin/ls
argv = "ls" "-la" "dipanf"